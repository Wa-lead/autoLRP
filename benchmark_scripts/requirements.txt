torch>=2.1
transformers>=4.40
datasets>=2.18
captum>=0.7
scikit-learn>=1.3
torchvision>=0.16
tqdm
numpy
# AttnLRP baseline only:
# lxt
# autoLRP: install your local package (pip install -e path/to/autoLRP) or pass --autolrp-repo
